<?php $this->layout('layout::home-usuario'); ?>

<?php
/** @var array $turma */
/** @var array $monitores */
/** @var array $atletas */
/** @var string $dataHoje */
// Fallback para evitar erro caso as variáveis não existam
$lista_monitores = $monitores ?? $this->monitores ?? [];
?>

<div class="mt-2 bg-white shadow-lg rounded-lg overflow-hidden">
    <h1 class="text-lg text-blue-600 mb-2 border-b p-3">
        Chamada da Aula <?= $dataHoje ?> da turma: <?= htmlspecialchars($turma['nome'] ?? '') ?>
    </h1>

    <form method="POST" class="p-3 space-y-6">
        <input type="text" name="monitor" value="<?= $this->escapeHtml($turma['monitor']) ?>" class="hidden">

        <!-- CABEÇALHO DA AULA (Editável) -->
       <div class="flex flex-wrap bg-gray-50 rounded-md gap-3 p-2">
            <div class="flex-1 min-w-25">
                <label class="block text-gray-500 text-xs">
                    Nível
                </label>
                <input type="text" name="nivel" value="<?= htmlspecialchars($turma['nivel'] ?? '') ?>" readonly="readonly" class="border border-gray-300 rounded-md px-2 w-full focus:outline-none focus:ring-1 focus:ring-blue-300 focus:border-blue-500">
            </div>
            <div class="flex-1 min-w-25">
                <label class="block text-gray-500 text-xs">
                    Local
                </label>
                <input type="text" name="local" value="<?= htmlspecialchars($turma['local'] ?? '') ?>" class="border border-gray-300 rounded-md px-2 w-full focus:outline-none focus:ring-1 focus:ring-blue-300 focus:border-blue-500">
            </div>
            <div class="flex-1 min-w-25">
                <label class="block text-gray-500 text-xs">
                    Dia
                </label>
                <input type="text" name="dia" value="<?= htmlspecialchars($turma['dia'] ?? '') ?>" class="border border-gray-300 rounded-md px-2 w-full focus:outline-none focus:ring-1 focus:ring-blue-300 focus:border-blue-500">
            </div>
            <div class="flex-1 min-w-25">
                <label class="block text-gray-500 text-xs">
                    Início
                </label>
                <input type="text" name="inicio" value="<?= htmlspecialchars($turma['inicio_formatado'] ?? '') ?>" class="border border-gray-300 rounded-md px-2 w-full focus:outline-none focus:ring-1 focus:ring-blue-300 focus:border-blue-500">
            </div>
           <div class="flex-1 min-w-25">
                <label class="block text-gray-500 text-xs">
                    Término
                </label>
                <input type="text" name="termino" value="<?= htmlspecialchars($turma['termino_formatado'] ?? '') ?>" class="border border-gray-300 rounded-md px-2 w-full focus:outline-none focus:ring-1 focus:ring-blue-300 focus:border-blue-500">
            </div>
            <div class="flex-1 min-w-25">
                <label class="block text-gray-500 text-xs">
                    Data
                </label>
                <input type="text" name="data" value="<?= $dataHoje ?>" class="border border-gray-300 rounded-md px-2 w-full focus:outline-none focus:ring-1 focus:ring-blue-300 focus:border-blue-500">
            </div>
        </div>

        <!-- MONITORES -->
        <div class="flex flex-wrap bg-gray-50 rounded-md gap-3 p-2">
            <datalist id="lista-monitor1">
                <?php foreach ($lista_monitores as $monitor): ?>
                    <option value="<?= $this->escapeHtml($monitor) ?>">
                    <?php endforeach; ?>
            </datalist>
            <div class="flex-1 min-w-25">
                <label class="block text-xs font-medium text-gray-700">
                    Monitor 1
                </label>
                <input type="text" name="monitor1" list="lista-monitores" value="<?= htmlspecialchars($turma['monitor'] ?? '') ?>" onfocus="this.value = ''" onblur="if(this.value=='') this.value='<?= htmlspecialchars($turma['monitor'] ?? '') ?>';" class="w-full indent-1 border rounded border-gray-300 focus:bg-gray-100 outline-none focus:border-blue-500">
            </div>
            <datalist id="lista-monitor2">
                <?php foreach ($lista_monitores as $monitor): ?>
                    <option value="<?= $this->escapeHtml($monitor) ?>">
                    <?php endforeach; ?>
            </datalist>
            <div class="flex-1 min-w-25">
                <label class="block text-xs font-medium text-gray-700">
                    Monitor 2
                </label>
                <input type="text" name="monitor2" list="lista-monitores" placeholder="Selecione..." onfocus="this.value = ''" class="w-full indent-1 border rounded border-gray-300 focus:bg-gray-100 outline-none focus:border-blue-500">
            </div>
        </div>

        <datalist id="lista-monitores">
            <?php foreach ($lista_monitores as $m): ?>
                <option value="<?= htmlspecialchars($m) ?>"></option>
            <?php endforeach; ?>
        </datalist>
        <div class="border rounded-lg overflow-hidden">
            <table class="w-full text-left border-collapse">
                <thead class="hidden md:table-header-group bg-blue-400 text-white">
                    <tr>
                        <th class="p-3 whitespace-nowrap">Presença</th>
                        <th class="p-3 whitespace-nowrap">Atleta</th>
                        <th class="p-3 whitespace-nowrap">Patins</th>
                        <th class="p-3 whitespace-nowrap">Responsável</th>
                        <th class="p-3 text-center whitespace-nowrap">Ações</th>
                    </tr>
                </thead>
                <tbody class="divide-y">
                    <?php if (!empty($atletas)): foreach ($atletas as $atleta):
                            $atleta['telefone'] = strlen($atleta['telefone']) === 11 ? '(' . substr($atleta['telefone'], 0, 2) . ') ' . substr($atleta['telefone'], 2, 5) . '-' . substr($atleta['telefone'], 5, 4) : '(' . substr($atleta['telefone'], 0, 2) . ') ' . substr($atleta['telefone'], 2, 4) . '-' . substr($atleta['telefone'], 6, 4);
                            $telLimpo = preg_replace('/\D/', '', $atleta['telefone'] ?? '');
                            $nomeAtleta = htmlspecialchars($atleta['nome'] ?? '');
                    ?>
                            <tr class="flex flex-col md:table-row hover:bg-gray-50 p-4 md:p-0">
                                <td class="md:p-3 flex justify-between items-center md:table-cell">
                                    <span class="md:hidden font-normal text-blue-600 text-xs uppercase">
                                        Presença
                                    </span>
                                    <input type="checkbox" name="atletas_presentes[]" value="<?= $nomeAtleta ?>" class="w-8 h-8 md:w-5 md:h-5 accent-blue-600 cursor-pointer" checked="checked">
                                </td>
                                <td class="md:p-3 py-1 md:table-cell">
                                    <span class="md:hidden block text-xs text-gray-400">
                                        ATLETA
                                    </span>
                                    <span class="font-medium text-gray-800">
                                        <?= $nomeAtleta ?>
                                    </span>
                                </td>
                                <td class="md:p-3 py-1 md:table-cell"><span class="md:hidden block text-xs text-gray-400">PATINS</span> <span class="bg-blue-100 text-blue-800 px-2 py-0.5 rounded text-sm font-medium md:bg-transparent md:p-0 md:font-normal"><?= htmlspecialchars($atleta['patins'] ?? '') ?></span></td>
                                <td class="md:p-3 py-1 md:table-cell">
                                    <span class="md:hidden block text-xs text-gray-400">
                                        RESPONSÁVEL
                                    </span>
                                    <div class="flex flex-col">
                                        <span class="text-sm">
                                            <?= htmlspecialchars($atleta['responsavel'] ?? '') ?>
                                        </span>
                                        <div class="flex flex-col">
                                            <span>
                                                <?= $this->escapeHtml($atleta['titular']) ?>
                                            </span>
                                            <span class="text-xs text-gray-400 md:text-gray-500">
                                                Fone: <?= $atleta['telefone'] ?? '---' ?>
                                            </span>
                                        </div>
                                    </div>
                                </td>
                                <td class="md:p-3 py-3 md:table-cell text-center border-t md:border-none">
                                    <?php if (!empty($telLimpo)): ?>
                                        <a href="https://wa.me/<?= $telLimpo ?>?text=Olá, estamos iniciando o treino de patinação com <?= $nomeAtleta ?> presente!" target="_blank" class="flex items-center justify-center gap-2 hover:bg-green-300 px-4 py-2 md:px-2 md:py-1 rounded-md transition-all text-xs text-gray-600 shadow-sm">
                                            <img src="/img/whats.png" title="WhatsApp do responsável pelo atleta" alt="logo do whatsApp">
                                            <span>
                                                WhatsApp do responsável: <?= $this->escapeHtml($atleta['titular']) ?>
                                            </span>
                                        </a>
                                    <?php else: ?>
                                        <span class="text-gray-400 italic text-xs">
                                            Sem número
                                        </span>
                                    <?php endif; ?>
                                </td>
                            </tr>
                    <?php endforeach;
                    endif; ?>
                </tbody>
            </table>
        </div>
        <div class="flex flex-col md:flex-row justify-between items-center gap-4 pt-4">
            <button type="button" onclick="window.history.back()" class="w-auto md:w-full px-12 py-1 bg-blue-400 text-white rounded font-medium hover:bg-blue-600 shadow-md cursor-pointer">
                Voltar
            </button>
            <button type="submit" class="w-auto md:w-full px-12 py-1 bg-blue-400 text-white rounded font-medium hover:bg-blue-600 shadow-md cursor-pointer">
                Salvar chamada
            </button>

        </div>
    </form>
</div>